# Documention of Unicorn engine

```
. <- 主要引擎core engine + README + 编译文档COMPILE.TXT 等
├── bindings <- 绑定
│ ├── go <- go 绑定 + 测试代码
│ ├── java <- Java 绑定 + 测试代码
│ ├── python <- Python 绑定 + 测试代码
├── docs <- Unicorn文档
├── include <- C头文件
├── qemu <- 魔改的qemu源码
├── samples <- Unicorn使用示例
└── tests <- C语言测试用例
```
